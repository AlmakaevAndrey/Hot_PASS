# sex-pass

Мини-проект: интерактивный персональный "digital sex pass" (18+). Стек: Vite + React + TypeScript + Tailwind + Framer Motion + Lucide.

Запуск:

```bash
cd sex-pass
npm install
npm run dev
```

Mobile-first дизайн; изменяй тексты и стили в `src/screens`.
